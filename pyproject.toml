[build-system]
requires = ["setuptools>=42", "wheel", "setuptools_scm"]
build-backend = "setuptools.build_meta"

[project]
name = "dnora"
version = "1.4.3"  
description = "dnora - a software for dynamical downscaling of wave products i.e., NORA3 wave hindcast and WAM4 wave forecast from MET Norway"
authors = [ { name = "Konstantinos Christakos"}, { name = "Jan-Victor Björkqvist" } ]
urls = {homepage = "https://github.com/MET-OM/dnora", download = "https://github.com/MET-OM/dnora"}

dependencies = [
    "matplotlib>=3.1",
    "numpy>=1.17",
    "scipy>=1.7",
    "netCDF4",
    "pandas",
    "xarray>=0.20.1",
    "meshio",
    "utm",
    "dask",
    "pytest",
    "cmocean",
    "cartopy",
    "cdsapi",
]

[tool.setuptools]
license-files = []
